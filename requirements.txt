# DEPRECATED — dependencies now live in pyproject.toml (PEP 621).
#
# Install options:
#   pip install -e ".[dashboard]"   # runtime + Streamlit dashboard
#   pip install -e ".[dev]"         # + ruff, pytest, mkdocs, pre-commit
#   pip install -e ".[test]"        # minimal CI surface
#
# CPU-only torch (keeps installs small on Linux servers/CI):
#   --extra-index-url https://download.pytorch.org/whl/cpu
